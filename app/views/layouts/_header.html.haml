
- if user_signed_in?
	%p= "Logged in as #{current_user.email}"
	%span= link_to "log out", destroy_user_session_path, method: :delete
	%li= button_to "Add a place", new_place_path, method: :get
- else
	%span= link_to "Sign in", new_user_session_path